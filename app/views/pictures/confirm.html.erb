<div class="fuield">
  <h3>以下の内容で、送信する。</h3>
  <%= form_with(model: @picture, url: confirm_new_or_edit, method: confirm_form_method, local: true) do |form| %>
    <%= form.hidden_field :title %>
    <%= form.hidden_field :content %>
    <p>タイトル:<%= @picture.title %></p>
    <p>本文:<%= @picture.content %></p>
    <p class="confirm_img"><%= image_tag(@picture.image.url) if @picture.image && @picture.image.url %></p>
    <%= form.hidden_field :image_cache %>
    <br>
    <%= form.submit "登録する" %>
  <% end %>
  <br><br>
  <%= form_with(model: @picture, url: confirm_new_or_edit, method: confirm_form_method, local:true) do |form| %>
    <%= form.hidden_field :title %>
    <%= form.hidden_field :content %>
    <%= form.hidden_field :image %>
    <%= form.hidden_field :image_cache %>
    <%= form.submit "戻る", name: "back" %>
  <% end %>
  <br>
</div>
